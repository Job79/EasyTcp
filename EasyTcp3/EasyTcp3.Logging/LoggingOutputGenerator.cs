using System.Text;

namespace EasyTcp3.Logging
{
    public class LoggingOutputGenerator
    {
        public static string GenerateOutput(LoggingMessage message)
        {
            StringBuilder builder = new StringBuilder();
            // TODO generate message
            return builder.ToString();
        }
    }
}